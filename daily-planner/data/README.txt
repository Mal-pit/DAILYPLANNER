This folder stores user data as JSON files.
- users.json          → all registered accounts (passwords hashed)
- user_<username>.json → per-user tasks, schedule, habits

Make sure this folder is writable by the web server (chmod 755 or 775).
